# ── Secrets & credentials ─────────────────────────────────────────────────────
.env
.env.*
!.env.example
secrets.*
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
*.jks
*.keystore
*_rsa
*_dsa
*_ecdsa
id_rsa
id_dsa
id_ecdsa
*.ppk
jwt_secret*
auth_token*

# ── Token / config files ───────────────────────────────────────────────────────
git_token.txt
*_token.txt
*_credentials.json
service_account*.json
gcp_key*.json
aws_credentials
.aws/

# ── Scope & user-specific target files ────────────────────────────────────────
scope.txt
targets.txt
*.scope

# ── Generated state & doctor reports ──────────────────────────────────────────
.doctor_state.json
*.state.json

# ── Scan outputs, reports & artifacts ─────────────────────────────────────────
output/
reports/
*.html
!web/frontend/index.html
!web/frontend/dist/index.html
*.log
*.log.*
debug_*.json
raw_*.json
scan_results/
/recon/

# ── Database files ─────────────────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
*.sql
!src/oneinfinity/db/schema.sql
*.bak
*.dump

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
dist/
build/
*.so
*.dylib
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# ── Virtual environments ───────────────────────────────────────────────────────
venv/
.venv/
env/
.env_dir/
web/backend/.venv/

# ── Node / Frontend ────────────────────────────────────────────────────────────
node_modules/
web/frontend/node_modules/
web/frontend/dist/

# ── OS artifacts ───────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# ── IDE & editor ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.sublime-project
*.sublime-workspace


# ── Compiled build artifacts (NEVER commit binaries) ──────────────────────────
src/rust/**/target/
src/ebpf/*.bpf.o
src/nim/bin/
src/go/oi-*/oi-*
src/go/http2_rapid_reset/http2_rapid_reset
src/go/mcp_scanner/mcp_scanner
checksums.json
# ── Postman collections (expose API surface) ────────────────────────────────────
gc_api.json
sc_api.json

# ── AI tool directories ────────────────────────────────────────────────────────
.claude/
.gemini/
.codex/
.opencode/
.cursor/
.superpowers/

# ── OneInfinity runtime data ───────────────────────────────────────────────────
.oneinfinity/
uploads/
tmp/
data/.gf/.src/
data/
oi-backtests/
.worktrees/
.cache/
logs/
web/backend/.venv/
web/frontend/node_modules/
web/frontend/dist/
web/frontend/build/

# ── Local development workspaces ─────────────────────────────────────────────
/core/
# Added by code-review-graph
.code-review-graph/

# Claude agent session data
.claude-flow/
oi-sync
/.oneinfinity/sync*.log

# ── DATA ISOLATION — findings/DB NEVER sync between environments ──────────────
# Each environment (laptop, EC2) keeps its own independent database and findings.
# NOTHING below this line should ever appear in git history.

# Scan findings and outputs
findings_fallback.jsonl
unified_findings.json
pipeline_report.json
advanced_findings.json
auth_findings.json
graphql_findings.json
browser_findings.json
swarm_findings.json
attack_simulation.json
validation_results.json
chains.json
attack_graph.json
zero_day.json
cicd_findings.json
container_findings.json
grpc_findings.json
smuggling_findings.json
oob_findings.json
target_registration.json
adaptive_recon.json
swarm_partial.json
suggested_chains.json

# Scan session state
god-mode-*.json
god-mode-*.stop

# Runtime DB / storage
*.jsonl
web/backend/data/
web/backend/reports/
screenshots/

# Local scan workspaces (laptop)
~/.oneinfinity/
