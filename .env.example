# ─────────────────────────────────────────────────────────────────────────────
# OneInfinity — Environment Configuration Template
# Copy this file to .env and fill in your values.
# NEVER commit .env or any file containing real credentials.
# ─────────────────────────────────────────────────────────────────────────────

# ── AI / LLM providers ────────────────────────────────────────────────────────
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# ── GitHub token(s) for secret intelligence scanning ─────────────────────────
# Comma-separated for token rotation support
GITHUB_TOKEN=
GITHUB_TOKENS=

# ── Backend API ───────────────────────────────────────────────────────────────
# Stable API key for the OneInfinity web backend.
# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# If unset, an ephemeral key is generated at startup (not suitable for production).
ONEINFINITY_API_KEY=

# Network binding (defaults: 127.0.0.1:8000)
API_HOST=127.0.0.1
API_PORT=8000
# Set to "true" only in local development
API_RELOAD=false

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=

# ── Optional proxy ────────────────────────────────────────────────────────────
PROXY_URL=

# ═══════════════════════════════════════════════════════════════
# Distributed Docker Compose settings (docker-compose.distributed.yml)
# ═══════════════════════════════════════════════════════════════

# Data & plugin directories (bind-mounted)
DATA_DIR=./data
PLUGIN_DIR=./plugins/community

# Container image tag (use 'latest-ai' for AI worker profile)
IMAGE_TAG=latest

# Public ports
HTTP_PORT=80
REDIS_PORT=6380
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
REDIS_UI_PORT=8081

# Worker concurrency (tasks per worker container instance)
RECON_CONCURRENCY=3
VULN_CONCURRENCY=2
EXPLOIT_CONCURRENCY=2
AI_CONCURRENCY=1

# Worker resource limits
WORKER_CPU_LIMIT=2.0
WORKER_MEM_LIMIT=2g

# Auto-update settings
UPDATE_SCHEDULE=0 4 * * *
NUCLEI_UPDATE=1
DOCKER_UPDATE=0
SLACK_WEBHOOK=

# GHCR credentials for Watchtower image pulls
GHCR_USER=
GHCR_TOKEN=

# Monitoring
GRAFANA_PASSWORD=admin
PROMETHEUS_RETENTION=30d

LOG_LEVEL=INFO
