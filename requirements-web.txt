# ============================================================
# OneInfinity — Web UI Backend Requirements
# Install only if using the FastAPI dashboard:
#   cd web/backend && uvicorn main:app
# ============================================================

-r requirements-core.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.0
aiofiles>=23.0
