# AI-Powered Offensive Security Research Framework : One&Infinity

An autonomous, AI-driven platform for offensive security research, bug bounty automation, mobile security testing, AI security testing, swarm-intelligence–driven vulnerability discovery, attack graph analysis, and **self-evolving architecture**.

_Auto-generated by `readme_generator.py`. Last updated: 2026-05-13._

---

## Features

### Web Security
- **Reconnaissance** — Subdomain enum, HTTP probing, JS endpoint extraction, cloud asset discovery
  - subfinder, amass, assetfinder, dnsx, httpx
- **Vulnerability Scanning** — nuclei v3 (175k+ templates), XSS, SQLi, SSRF, IDOR, CORS, CRLF, Open Redirect
  - dalfox, kxss, XSStrike, sqlmap, commix, crlfuzz
- **Secret Detection** — trufflehog, gitleaks, jwt_tool across repos and endpoints
- **API Security** — GraphQL injection, BOLA, mass assignment, rate limit bypass
- **Cloud Misconfiguration** — S3, Elasticsearch, K8s, credential leakage checks

### Mobile Security
- **12-Phase Pipeline** — APK static → AI reverse → Frida gen → secrets → API → components → dynamic → network → API attack → aggregate
- **Static Analysis** — APKTool + JADX + MobSF, 24 dangerous permissions, 14 vuln patterns
- **Runtime Analysis** — Frida auto-scripts, Objection, RMS — SSL bypass, root bypass, crypto hooks
- **Secret Detection** — Binary DEX string scanning, assets/gradle patterns, FP filtering

### AI Security
- **AI Vulnerability Scanners** — Garak + PyRIT integration
- **Red Team Campaigns** — Jailbreak (5000+ prompts), RAG poisoning, tool abuse, model extraction
- **Prompt Injection** — Detection + exploitation across LLM endpoints

### Swarm Intelligence
- **8 Specialized Agents** — XSS, SQLi, SSRF, IDOR, Auth Bypass, Business Logic, Mobile, API Security — parallel execution
- **Agent Collaboration** — Event bus: SQLi→IDOR, SSRF→IDOR, XSS→Auth triggers on finding confirmation
- **Monte Carlo Simulation** — N=200 Bernoulli trials, 23-path catalog, 6 probability factors, EV ranking
- **Workflow Simulation** — 4 workflow factories (checkout/login/reset/transfer), 9 business logic attack categories
- **EMA Learning** — α=0.30 per-agent success rate persistence across sessions

### Attack Graph Intelligence
- **Real-time Attack Graph** — Nodes: host/service/vuln/chain; edges: exploits/leads-to/chained-with
- **BFS Path Enumeration** — Attack path finding from target root to exploit nodes
- **Exploit Chain Detection** — 12 multi-step patterns: XSS→ATO, SQLi→RCE, SSRF→Cloud, etc.
- **Risk Scoring** — CVSS + exploitability + chain bonus × impact multiplier
- **OSINT Collection** — crt.sh, ASN, Wayback, GitHub dorks, Shodan, URLScan

### Autonomous Bug Bounty Hunter
- **Program Discovery** — HackerOne, Bugcrowd, Intigriti APIs
- **Target Prioritization** — 6-dimension weighted scoring (tech risk, cloud, sensitive paths)
- **Full Pipeline** — discover → recon → crawl → vuln_scan → exploit → validate → report
- **Report Templates** — HackerOne/Bugcrowd markdown with CVSS + bounty estimates

### Self-Evolving Architecture
- **Event-Driven Updates** — feature_added / scan_completed / vulnerability_discovered / exploit_chain_detected / tool_integrated events
- **Auto Doc Rewrite** — ARCHITECTURE.md, SKILLS.md, README.md updated automatically on activity
- **Persistent Memory** — evolution.db: attack patterns, exploit chains, learning insights, capability snapshots
- **System Evolution Dashboard** — React UI: recent updates, new capabilities, architecture changes, learning insights

### Exploit Development
- **Auto-Quantum Exploit Engine 1778637774** — A hypothetical quantum-based exploit engine for post-quantum crypto analysis.

---

## CLI

Primary command: `oneinfinity`

```bash
# Core Scanning
oneinfinity scan <target>                            # Full autonomous pentest (7-phase)
oneinfinity adaptive-recon <target>                  # Tech detect + JS endpoints + cloud assets
oneinfinity research <target> --yes                  # Autonomous research loop
```

```bash
# Swarm Intelligence
oneinfinity swarm-scan <target>                      # 8-agent parallel swarm scan
oneinfinity simulate-attacks <target>                # Monte Carlo attack path simulation (N=200)
oneinfinity simulate-workflow <target>               # Business logic workflow simulation
```

```bash
# Mobile
oneinfinity mobile-analyze app.apk                   # Full 12-phase mobile security pipeline
oneinfinity mobile-static app.apk                    # Static analysis only
oneinfinity mobile-secrets app.apk                   # Secret/credential scanning
```

```bash
# AI Security
oneinfinity ai-test <target> --all                   # All AI security tools
oneinfinity ai-redteam <target> --campaign jailbreak --prompts 5000 # AI red team campaign
```

```bash
# Autonomous Hunter
oneinfinity hunter-start                             # Discover programs + auto-scan
oneinfinity hunter-scan <target>                     # Single-target pipeline
```

```bash
# Attack Graph
oneinfinity attack-graph <target>                    # Build + view attack graph
oneinfinity agents run <target> --yes                # Multi-agent pentest
```

```bash
# Self-Evolving Architecture
oneinfinity arch-status                              # Show evolution engine status + recent changes
oneinfinity arch-events                              # List recent architecture events
oneinfinity arch-insights                            # Show latest learning insights
```

```bash
# Utilities
oneinfinity toolcheck                                # Check tool availability
oneinfinity learn stats                              # Learning system statistics
oneinfinity profile list                             # List scan profiles
oneinfinity cache stats                              # Recon cache statistics
```

---

## Web UI

Start backend + frontend:

```bash
./web/start.sh
```

| Service | URL |
|---------|-----|
| React dashboard | `http://localhost:3000` |
| FastAPI REST    | `http://localhost:8000` |
| OpenAPI docs    | `http://localhost:8000/docs` |

### Pages

| Page | Route | Description |
|------|-------|-------------|
| **Dashboard** | `/dashboard` | Overview metrics, recent findings |
| **Targets** | `/targets` | Target management |
| **Scans** | `/scans` | Scan history and live progress |
| **Vulnerabilities** | `/vulnerabilities` | Finding browser with filters |
| **Attack Graph** | `/attack-graph` | Graph visualization |
| **Graph Explorer** | `/graph-explorer` | ForceGraph2D, attack paths, risk report |
| **AI Red Team** | `/ai-redteam` | Campaign builder and results |
| **AI Security** | `/ai-security` | AI test console |
| **AI Agent Pentest** | `/ai-agent-pentest` | Agentic system tester |
| **Mobile Security** | `/mobile` | 14-tab mobile analysis UI |
| **Bounty Hunter** | `/hunter` | Autonomous hunter (5-tab) |
| **Swarm Intelligence** | `/swarm-intelligence` | Swarm scan, Monte Carlo sim, workflow sim, history |
| **System Evolution** | `/evolution` | Architecture changes, new capabilities, insights |
| **Traffic Explorer** | `/traffic` | HTTP capture and replay |
| **Attack Replay** | `/attack-replay` | Attack replay builder |
| **Reports** | `/reports` | Report viewer |
| **Settings** | `/settings` | Platform configuration |

---

## Architecture

```
oneinfinity.py (CLI, 58 commands)
    │
    ├── Self-Evolving Architecture (NEW)
    │   ├── auto_architecture_engine.py  — event bus, dispatcher, integration hooks
    │   ├── architecture_updater.py       — ARCHITECTURE.md section patcher
    │   ├── memory_manager.py             — evolution.db: patterns/chains/insights
    │   ├── skills_tracker.py             — SKILLS.md capability registry
    │   └── readme_generator.py           — README.md auto-generator
    │
    ├── Swarm Intelligence
    │   ├── swarm_intelligence_engine.py  — 8 specialized agents + EMA learning
    │   ├── agent_swarm_coordinator.py    — parallel orchestration + event bus
    │   ├── attack_simulation_engine.py   — Monte Carlo N=200 simulations
    │   └── workflow_simulation_engine.py — business logic attack simulation
    │
    ├── Core Agents
    │   ├── agents/coordinator.py         — orchestrator, sequential phases
    │   ├── agents/recon_agent.py         — subfinder/httpx/katana/waybackurls
    │   ├── agents/scan_agent.py          — nuclei/dalfox/sqlmap/trufflehog
    │   └── agents/report_agent.py        — HackerOne/Bugcrowd reports
    │
    ├── Intelligence Engines
    │   ├── adaptive_recon_engine.py      — tech detect, JS endpoints, cloud
    │   ├── application_intelligence.py   — AppModel: auth flows, API structure
    │   ├── vulnerability_theory_engine.py — 23 rule-based vuln theories
    │   └── attack_graph_core/            — graph engine, risk analyzer, planner
    │
    ├── Mobile Security
    │   ├── mobile_security_engine.py     — 12-phase pipeline
    │   ├── mobile_static_analysis.py     — APKTool + JADX + MobSF
    │   ├── frida_script_generator.py     — auto-generate Frida JS
    │   └── mobile_dynamic_analysis.py    — Frida + Objection runtime
    │
    ├── AI Security
    │   └── ai_security/                  — Garak, PyRIT, prompt injection, campaigns
    │
    └── Web Layer
        ├── web/backend/main.py           — FastAPI, 60+ routes, WebSocket
        └── web/frontend/src/             — React 18, Tailwind, Recharts, Zustand
```

---

## Data Directory

Default: `~/.oneinfinity`

Override: `ONEINFINITY_HOME` or `ONEINFINITY_DATA_DIR` environment variables.


## Tool Requirements

**Go tools** (`~/go/bin/`): dalfox, crlfuzz, kxss, dnsx, naabu, gobuster, ffuf, anew, gf, qsreplace, httpx, waybackurls, gauplus, katana, hakrawler, subfinder, assetfinder, amass, nuclei

**Binaries** (`~/.local/bin/`): trufflehog, gitleaks, findomain

**Git clones** (`~/.local/`): sqlmap, xssstrike, jwt_tool, commix, paramspider, dirsearch

**Python packages**: arjun, s3scanner, sublist3r, whatweb, nikto, garak, pyrit


